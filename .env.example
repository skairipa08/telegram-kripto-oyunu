# Documentation only: Vite reads apps/web/.env, Wrangler reads apps/api/.dev.vars.
# R0 requires no secrets. Never put private credentials in VITE_ variables.
# These server-only variables will be wired in subsequent steps.
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
SESSION_SECRET=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
