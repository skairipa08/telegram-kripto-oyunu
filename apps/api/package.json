{
  "name": "@empire/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1 --port 8787",
    "typecheck": "tsc -p tsconfig.json",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@empire/game-core": "workspace:*",
    "@empire/shared": "workspace:*",
    "hono": "^4.10.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@ton/core": "^0.63.1",
    "@ton/crypto": "^3.3.0",
    "wrangler": "^4.131.2"
  }
}
